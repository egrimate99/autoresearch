dataset:
  split: train
  num_scrs: 512
  only_implementable: true

domain:
  n_agents: 3
  n_states: 4
  n_alternatives: 4
  preference_type: ordinal

mechanism:
  message_scaffold: maskin
  challenge_size: 4
  deterministic_outcomes: true

training:
  seed: 0
  epochs: 200
  batch_size: 16
  lr: 0.001
  hidden_dim: 64
  device: cpu
